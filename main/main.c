#include <SDL2/SDL.h>
#include <math.h>
#include "sdl_init.h"
#include "defs.h"

int main(void){
/*
    SDL_Renderer* renderer = Init_SDL();

    // INITS //////////////////////////////////////////////////////////////////////



    ///////////////////////////////////////////////////////////////////////////////

    SDL_Event event;
    int running = 1;
    int follow = 0;
    while (running){
        if (SDL_PollEvent(&event)) {
            switch (event.type)
            {
            case SDL_QUIT:
                running = 0;
                continue;


            default:
                break;
            }
        }

    }
*/



    return 0;
}
